function vehicle(name, maker, engine, speed) {
    this.name = name;
    this.maker = maker;
    this.engine = engine;
    this.speed = speed;
}
var car = new vehicle("Gixxer", "Suzuzki", "9000cc", 2000);
console.log(car.name);
console.log(car.speed);
